<%= form_for([:admin,@admin_box]) do |f| %>
  <% if @admin_box.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_box.errors.count, "error") %> prohibited this admin_box from being saved:</h2>

      <ul>
      <% @admin_box.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, :rows => 5, :cols => 20 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
