<%= form_for(@admin_idea) do |f| %>
  <% if @admin_idea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_idea.errors.count, "error") %> prohibited this admin_idea from being saved:</h2>

      <ul>
      <% @admin_idea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
