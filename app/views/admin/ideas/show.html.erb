<p id="notice"><%= notice %></p>
<%= render @admin_idea %>
<%= link_to 'Edit', edit_admin_box_idea_path(@admin_box,@admin_idea) %> |
<%= link_to 'Back', admin_box_path(@admin_box) %>

<div class="comments">
  <% if !@admin_idea.comments.nil? %>
    <%= render @admin_idea.comments %>
  <% end %>
</div>
<div class="comment-form">
  <% if !current_user.nil? %>
    <%= render "admin/comments/form" %>
  <% end %>
</div>
