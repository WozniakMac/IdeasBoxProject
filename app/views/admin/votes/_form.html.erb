<%= form_for(@admin_vote) do |f| %>
  <% if @admin_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_vote.errors.count, "error") %> prohibited this admin_vote from being saved:</h2>

      <ul>
      <% @admin_vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
