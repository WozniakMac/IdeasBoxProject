<%= form_for(@manager_box) do |f| %>
  <% if @manager_box.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manager_box.errors.count, "error") %> prohibited this manager_box from being saved:</h2>

      <ul>
      <% @manager_box.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
