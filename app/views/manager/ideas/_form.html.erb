<%= form_for([:manager,@manager_box,@manager_idea]) do |f| %>
  <% if @manager_idea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manager_idea.errors.count, "error") %> prohibited this manager_idea from being saved:</h2>

      <ul>
      <% @manager_idea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, :rows => 5, :cols => 20 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
