<%= form_for(@manager_vote) do |f| %>
  <% if @manager_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manager_vote.errors.count, "error") %> prohibited this manager_vote from being saved:</h2>

      <ul>
      <% @manager_vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
